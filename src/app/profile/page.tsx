import { Metadata } from "next";
import { title } from "process";

export const metadata = {
  title: "Profile"
}

export default function  Profile()  {
  return <div>This is Profile page</div>;
};




// using title metadata - "template" key ; see rootLayout



